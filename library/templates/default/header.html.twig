<header class="header">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <a class="navbar-brand logo_font" href="{{ path('book_index') }}"><span class="logo_red">L</span>ibrary</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNavDropdown">
            <ul class="navbar-nav justify-content-end col-6">
                {% if app.user %}
                    <li class="nav-item default_font col-6 col-xl-4 p-0">
                        <a class="nav-link text-truncate" href="#">Привет, {{ app.user.name }}</a>
                    </li>
                    <li class="nav-item default_font col-5">
                        <a class="nav-link" href="{{ path('book_new') }}">Добавить книгу</a>
                    </li>
                    <li class="nav-item default_font col-3">
                        <a class="nav-link" href="{{ path('app_logout') }}">Выйти</a>
                    </li>
                {% else %}
                    <li class="nav-item default_font col-3 text-center">
                        <a class="nav-link" href="{{ path('app_login') }}">Войти</a>
                    </li>
                    <li class="nav-item default_font col-3 text-center">
                        <a class="nav-link" href="{{ path('app_register') }}">Регистрация</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </nav>
</header>